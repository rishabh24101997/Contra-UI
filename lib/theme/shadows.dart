import 'package:Contra/theme/colors.dart';
import 'package:flutter/material.dart';

double off200 = 2;
double off400 = 4;
double off600 = 6;

BoxShadow shadow200 =
    BoxShadow(blurRadius: 0, offset: Offset(0, off200), color: blackColor);

BoxShadow shadow400 =
    BoxShadow(blurRadius: 0, offset: Offset(0, off400), color: blackColor);

BoxShadow shadow600 =
    BoxShadow(blurRadius: 0, offset: Offset(0, off600), color: blackColor);

BoxShadow shadowM200 =
    BoxShadow(blurRadius: 0, offset: Offset(0, -off200), color: blackColor);

BoxShadow shadowM400 =
    BoxShadow(blurRadius: 0, offset: Offset(0, -off400), color: blackColor);

BoxShadow shadowM600 =
    BoxShadow(blurRadius: 0, offset: Offset(0, -off600), color: blackColor);

// BoxShadow pxLine =
//     BoxShadow(blurRadius: 0, offset: Offset(0, -1), color: blackColor);
